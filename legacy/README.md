# 旧版文件归档

`upstream/Bat_To_Exe_Converter_x64.exe` 是原仓库中的历史二进制文件，仅用于保留项目来源和行为对照。

当前开源实现位于 `src/BatToExeConverter.Cn/`，从源码独立构建，不读取、不调用也不依赖该旧版 EXE。新用户无需运行 `legacy/` 下的任何文件。

公开发布时建议把新版构建产物放在 GitHub Releases，而不是继续向源码目录提交新的二进制文件。
